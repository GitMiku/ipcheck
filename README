Just a simple bash script to check your public IP address. It runs in an infinite loop because I have uses for such a thing. If you don't then just remove the loop.

CTRL+C to end the script.

Mostly posting this to test the waters of git. 
